# This file is for documentation purposes only.
# It contains which selector field used in each test
[stats]
node=node

[summary]
node=node
container=container-name
pod=pod-name

[cAdvisor]
container=container_name
pod=pod_name

[aggregated]
container=name
pod=name
namespace=names
cluster=cluster_name
node=node